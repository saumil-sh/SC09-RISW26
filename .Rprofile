# Use more CPU cores while building packages
options(Ncpus = parallel::detectCores())

# Use the public Posit Package Manager snapshot (cross-platform binaries)
options(repos = c(PPM = "https://packagemanager.posit.co/cran/2026-08-27"))

source("renv/activate.R")
